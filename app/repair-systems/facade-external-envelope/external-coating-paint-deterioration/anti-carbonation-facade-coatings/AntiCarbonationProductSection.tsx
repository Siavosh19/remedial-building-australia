"use client";

import { useState, useRef } from "react";
import {
  CheckCircle, AlertTriangle, BookOpen, Layers, SquareStack,
  Ruler, ExternalLink, ChevronDown, ChevronUp,
  XCircle, ChevronLeft, ChevronRight, FileText,
} from "lucide-react";

type FilterTag =
  | "Anti-carbonation"
  | "Facade-coating"
  | "Concrete"
  | "Masonry"
  | "Elastomeric"
  | "CO2-barrier"
  | "Carbonation-protection"
  | "Water-repellent";

type Product = {
  fullLabel: string;
  brandUrl: string;
  tdsUrl?: string;
  accentColor: string;
  name: string;
  descriptionLine: string;
  productType: string;
  filterTags: FilterTag[];
  techChips: { label: string; cls: string }[];
  systemDescription: string;
  technicalProperties: string[];
  limitations: string[];
  procurementSources: { name: string; url: string }[];
};

const PRODUCTS: Product[] = [
  {
    fullLabel: "ARDEX Australia",
    brandUrl: "https://www.ardexaustralia.com",
    tdsUrl: "https://www.ardexaustralia.com",
    accentColor: "#f97316",
    name: "ARDEX WPM 330 Facade",
    descriptionLine: "Elastomeric anti-carbonation facade coating — CO2 barrier for concrete and masonry facades — protects reinforcing steel from carbonation-induced corrosion — confirm current specifications with ARDEX Australia technical",
    productType: "Elastomeric anti-carbonation facade protective coating",
    filterTags: ["Anti-carbonation", "Facade-coating", "Concrete", "Masonry", "Elastomeric", "CO2-barrier", "Carbonation-protection"],
    techChips: [
      { label: "Anti-carbonation", cls: "bg-orange-100 text-orange-800" },
      { label: "CO2 barrier", cls: "bg-orange-100 text-orange-800" },
      { label: "Elastomeric", cls: "bg-sky-100 text-sky-800" },
      { label: "Facade", cls: "bg-slate-100 text-slate-700" },
    ],
    systemDescription:
      "ARDEX WPM 330 Facade is an elastomeric anti-carbonation protective coating for concrete and masonry facades. Anti-carbonation coatings are specified to provide a barrier against CO2 ingress into concrete — CO2 reacts with calcium hydroxide in concrete to form calcium carbonate, which progressively reduces the pH at the reinforcing steel and removes the passive protection layer, leading to corrosion initiation (carbonation-induced spalling).\n\nElastomeric anti-carbonation coatings provide a flexible CO2 and water-vapour-resistant barrier while accommodating the micro-crack movement typical in facade concrete. They are used on concrete frame buildings and precast concrete facades as a protective overcoat after concrete repair, or as a preventive coating on buildings at risk of carbonation progression.\n\nConfirm current product technical data sheet, system design requirements, CO2 equivalent air thickness (sd value), application method, primer requirements, and DFT with ARDEX Australia technical before specifying. Do not rely on this general reference as a substitute for current ARDEX WPM 330 Facade technical documentation.",
    technicalProperties: [
      "Elastomeric anti-carbonation coating — provides CO2 barrier to slow carbonation progression in concrete facades",
      "Flexible formulation — accommodates micro-crack movement in facade concrete and precast elements",
      "Suitable for concrete and masonry facade substrates — confirm primer requirements with ARDEX Australia",
      "Confirm sd value (CO2 equivalent air thickness), DFT, coverage, and application conditions from current ARDEX WPM 330 Facade TDS",
      "Confirm current AS/NZS or ISO carbonation standard compliance references from current ARDEX documentation",
    ],
    limitations: [
      "Anti-carbonation coating alone cannot reverse existing carbonation — active structural repairs must be completed before coating application",
      "Confirm primer specification and substrate preparation requirements with ARDEX Australia before specifying",
      "Not a substitute for structural concrete repair — specify in conjunction with a concrete repair specification where spalling or corrosion is present",
      "Confirm current product availability, system documentation and technical support with ARDEX Australia before specifying",
    ],
    procurementSources: [
      { name: "ARDEX Australia — trade supply and technical", url: "https://www.ardexaustralia.com" },
    ],
  },
];

const FILTER_DEFS: { id: FilterTag; label: string }[] = [
  { id: "Anti-carbonation", label: "Anti-carbonation" },
  { id: "Facade-coating", label: "Facade coating" },
  { id: "Concrete", label: "Concrete" },
  { id: "Masonry", label: "Masonry" },
  { id: "Elastomeric", label: "Elastomeric" },
  { id: "CO2-barrier", label: "CO2 barrier" },
];

const TECH_INFO = {
  typicalApplications: [
    "Protective coating on concrete frame facade buildings after carbonation-induced spalling repair",
    "Preventive coating on aging concrete facades at risk of carbonation progression",
    "Overcoat on precast concrete panels after patch repair of carbonation spalling",
    "Protective coating on reinforced concrete facades in urban environments with elevated CO2 levels",
  ],
  selectionCriteria: [
    "Specify anti-carbonation coating where carbonation depth has been measured and is approaching the reinforcing steel cover depth",
    "Select elastomeric grade for facades with micro-cracking or expected thermal movement",
    "Confirm the coating sd value (CO2 equivalent air layer thickness) is sufficient for the required protection period",
    "Always complete structural concrete repair before applying anti-carbonation coating",
    "Confirm primer requirements for the specific substrate with ARDEX Australia before specifying",
  ],
  limitations: [
    "Anti-carbonation coating does not repair existing concrete damage — structural repairs must precede coating application",
    "Coating system effectiveness depends on correct DFT — confirm and enforce minimum film thickness requirements",
    "Not suitable for substrates with active moisture transmission or water ingress — address waterproofing defects before coating",
    "Confirm product compatibility with existing coatings if overcoating an existing painted facade",
  ],
  standardsNotes: [
    "EN 1504-2 — Protection and repair of concrete structures — surface protection systems — European reference standard",
    "AS 3600 — Concrete structures — confirm cover depth requirements for the applicable exposure classification",
    "Confirm current ARDEX system documentation and compliance references with ARDEX Australia technical",
  ],
  suitableDefects: [
    "Carbonation-induced concrete spalling — after structural repair, as preventive overcoat",
    "Concrete facade deterioration in high-CO2 urban environments — preventive maintenance coating",
    "Post-repair protective coating on concrete frame buildings after patch spalling repair programme",
  ],
  typicalSubstrates: [
    "In-situ reinforced concrete facade — confirm surface preparation and primer requirements",
    "Precast concrete panels — confirm system compatibility with ARDEX Australia",
    "Concrete masonry — confirm system suitability for the substrate with ARDEX technical",
  ],
};

function CollapsibleList({
  items,
  icon,
  limit = 3,
}: {
  items: string[];
  icon: "check" | "x";
  limit?: number;
}) {
  const [expanded, setExpanded] = useState(false);
  const visible = expanded ? items : items.slice(0, limit);
  const extra = items.length - limit;
  return (
    <div>
      <ul className="space-y-1.5">
        {visible.map((item, i) => (
          <li key={i} className="flex items-start gap-2 text-xs leading-5 text-slate-600">
            {icon === "check" ? (
              <CheckCircle size={12} className="mt-0.5 shrink-0 text-green-500" />
            ) : (
              <XCircle size={12} className="mt-0.5 shrink-0 text-red-400" />
            )}
            {item}
          </li>
        ))}
      </ul>
      {items.length > limit && (
        <button
          onClick={() => setExpanded((e) => !e)}
          className="mt-2 text-[10px] font-bold text-slate-400 hover:text-slate-600"
        >
          {expanded ? "Show less ↑" : `+${extra} more ↓`}
        </button>
      )}
    </div>
  );
}

function CollapsibleSources({ sources }: { sources: { name: string; url?: string }[] }) {
  const [expanded, setExpanded] = useState(false);
  return (
    <div>
      <div className="flex items-center justify-between">
        <p className="text-[9px] font-bold uppercase tracking-wider text-slate-400">PROCUREMENT SOURCES</p>
        <button
          onClick={() => setExpanded((e) => !e)}
          className="text-[9px] font-bold text-slate-400 hover:text-slate-600"
        >
          {expanded ? "Hide ↑" : "See more ↓"}
        </button>
      </div>
      {expanded && (
        <div className="mt-2 space-y-1.5">
          {sources.map((src) => (
            <div
              key={src.name}
              className="flex items-center gap-2 rounded-lg border border-slate-200 bg-white px-3 py-1.5 text-xs"
            >
              {src.url ? (
                <a
                  href={src.url}
                  target="_blank"
                  rel="noopener noreferrer"
                  className="flex items-center gap-1 font-semibold text-slate-700 hover:text-slate-900"
                >
                  {src.name}
                  <ExternalLink size={9} className="text-slate-300" />
                </a>
              ) : (
                <span className="font-semibold text-slate-600">{src.name}</span>
              )}
            </div>
          ))}
        </div>
      )}
      <p className="mt-2 text-[10px] italic text-slate-400">
        Confirm suitability with the current manufacturer TDS before specifying or applying.
      </p>
    </div>
  );
}

function CollapsibleCardDetails({
  text,
  chips,
}: {
  text: string;
  chips: { label: string; cls: string }[];
}) {
  const [expanded, setExpanded] = useState(false);
  return (
    <div>
      {expanded && (
        <>
          <p className="mt-1 text-[10px] leading-4 text-slate-500">{text}</p>
          {chips.length > 0 && (
            <div className="mt-2 flex flex-wrap gap-1.5">
              {chips.map((chip) => (
                <span key={chip.label} className={`rounded-full px-2.5 py-0.5 text-[10px] font-bold ${chip.cls}`}>
                  {chip.label}
                </span>
              ))}
            </div>
          )}
        </>
      )}
      <button
        onClick={() => setExpanded((e) => !e)}
        className="mt-0.5 text-[9px] font-bold text-slate-400 hover:text-slate-600"
      >
        {expanded ? "Hide details ↑" : "Show details ↓"}
      </button>
    </div>
  );
}

function CollapsibleDescription({ text }: { text: string }) {
  const [expanded, setExpanded] = useState(false);
  return (
    <div>
      <p className={`whitespace-pre-line text-xs leading-6 text-slate-700 ${expanded ? "" : "line-clamp-4"}`}>
        {text}
      </p>
      <button
        onClick={() => setExpanded((e) => !e)}
        className="mt-1.5 text-[10px] font-bold text-sky-700 hover:text-sky-900"
      >
        {expanded ? "Show less ↑" : "Show more ↓"}
      </button>
    </div>
  );
}

export function AntiCarbonationIntroSection() {
  const [expanded, setExpanded] = useState(false);
  return (
    <div className="rounded-2xl border border-slate-200 bg-white p-7 shadow-sm">
      <div className="mb-4 flex items-center gap-2.5">
        <div className="flex h-8 w-8 items-center justify-center rounded-lg bg-sky-950 text-white">
          <BookOpen size={15} />
        </div>
        <h3 className="text-base font-extrabold text-sky-950">
          What are anti-carbonation facade coatings?
        </h3>
      </div>
      <div className="space-y-4 text-sm leading-7 text-slate-600">
        <p>
          Anti-carbonation facade coatings are elastomeric protective coatings applied to concrete and masonry facades to provide a barrier against carbon dioxide (CO2) ingress. Carbonation occurs when CO2 from the atmosphere reacts with calcium hydroxide in concrete, reducing the concrete pH and progressively neutralising the alkaline environment that protects reinforcing steel from corrosion.
        </p>
        {expanded && (
          <>
            <p>
              When carbonation depth reaches the reinforcing steel cover depth, the passive protection layer on the steel surface is removed, allowing chloride-initiated or moisture-driven corrosion to begin. Anti-carbonation coatings are specified to slow this process by reducing the rate of CO2 diffusion through the concrete cover. They are applied after structural repairs are completed as a protective overcoat, or as a preventive maintenance treatment on aging concrete facades.
            </p>
            <p>
              The effectiveness of an anti-carbonation coating is characterised by its sd value — the equivalent air layer thickness for CO2 diffusion. A coating with a high sd value (&gt;50 m) provides meaningful protection against carbonation progression. Elastomeric formulations accommodate the micro-crack movement typical in exposed concrete facades. Confirm system design, DFT, primer requirements, and compliance references with ARDEX Australia before specifying.
            </p>
          </>
        )}
      </div>
      <button
        onClick={() => setExpanded((e) => !e)}
        className="mt-4 text-xs font-bold text-sky-700 hover:text-sky-900"
      >
        {expanded ? "Read less ↑" : "Read more ↓"}
      </button>
    </div>
  );
}

function TechCard({
  icon,
  title,
  items,
  style,
}: {
  icon: React.ReactNode;
  title: string;
  items: string[];
  style: "bullet" | "check" | "warn";
}) {
  return (
    <div className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm">
      <div className="mb-4 flex items-center gap-2.5">
        <div className="flex h-7 w-7 shrink-0 items-center justify-center rounded-lg bg-sky-950 text-white">
          {icon}
        </div>
        <h3 className="text-sm font-extrabold text-sky-950">{title}</h3>
      </div>
      <ul className="space-y-2">
        {items.map((item, i) => (
          <li key={i} className="flex items-start gap-2.5 text-xs leading-5 text-slate-600">
            {style === "check" && <CheckCircle size={12} className="mt-0.5 shrink-0 text-green-600" />}
            {style === "warn" && <AlertTriangle size={12} className="mt-0.5 shrink-0 text-amber-500" />}
            {style === "bullet" && <span className="mt-1.5 h-1.5 w-1.5 shrink-0 rounded-full bg-red-600" />}
            {item}
          </li>
        ))}
      </ul>
    </div>
  );
}

export function AntiCarbonationProductSection() {
  const [accordionOpen, setAccordionOpen] = useState(false);
  const [activeFilters, setActiveFilters] = useState<Set<FilterTag>>(new Set());
  const scrollRef = useRef<HTMLDivElement>(null);

  const toggleFilter = (id: FilterTag) => {
    setActiveFilters((prev) => {
      const next = new Set(prev);
      if (next.has(id)) next.delete(id);
      else next.add(id);
      return next;
    });
  };

  const visibleProducts =
    activeFilters.size === 0
      ? PRODUCTS
      : PRODUCTS.filter((p) =>
          Array.from(activeFilters).every((f) => p.filterTags.includes(f))
        );

  const scroll = (dir: "left" | "right") => {
    scrollRef.current?.scrollBy({ left: dir === "right" ? 400 : -400, behavior: "smooth" });
  };

  return (
    <>
      <div className="rounded-2xl border border-slate-200 bg-white shadow-sm overflow-hidden">
        <button
          type="button"
          onClick={() => setAccordionOpen((o) => !o)}
          className="flex w-full items-center justify-between gap-4 px-7 py-5 text-left transition hover:bg-slate-50"
        >
          <div>
            <p className="text-base font-extrabold text-sky-950">System Technical Reference</p>
            <p className="mt-0.5 text-xs text-slate-500">Applications, selection criteria, limitations, standards, suitable substrates</p>
          </div>
          <div className="flex shrink-0 items-center gap-2 rounded-lg border border-slate-200 bg-slate-50 px-3 py-1.5 text-xs font-bold text-slate-500">
            {accordionOpen ? <>Hide detail <ChevronUp size={14} /></> : <>Show detail <ChevronDown size={14} /></>}
          </div>
        </button>
        {accordionOpen && (
          <div className="border-t border-slate-100 px-7 pb-7 pt-6">
            <div className="grid gap-6 md:grid-cols-2 xl:grid-cols-3">
              <TechCard icon={<Layers size={15} />} title="Typical Applications" items={TECH_INFO.typicalApplications} style="bullet" />
              <TechCard icon={<Ruler size={15} />} title="Selection Criteria" items={TECH_INFO.selectionCriteria} style="check" />
              <TechCard icon={<AlertTriangle size={15} />} title="When NOT to Use" items={TECH_INFO.limitations} style="warn" />
              <TechCard icon={<BookOpen size={15} />} title="Standards & Notes" items={TECH_INFO.standardsNotes} style="bullet" />
              <TechCard icon={<CheckCircle size={15} />} title="Suitable Defects" items={TECH_INFO.suitableDefects} style="check" />
              <TechCard icon={<SquareStack size={15} />} title="Typical Substrates" items={TECH_INFO.typicalSubstrates} style="bullet" />
            </div>
          </div>
        )}
      </div>

      <div>
        <div className="mb-5 flex items-start gap-3">
          <div className="mt-1 h-5 w-1 shrink-0 rounded-full bg-red-700" />
          <div>
            <h2 className="text-2xl font-extrabold text-sky-950">Product Reference</h2>
            <p className="mt-1 text-sm text-slate-500">1 product — ARDEX — anti-carbonation facade protective coating systems</p>
          </div>
        </div>

        <div className="mb-5 flex flex-wrap items-center gap-2">
          <span className="shrink-0 text-xs font-semibold text-slate-500">Filter by:</span>
          {FILTER_DEFS.map((f) => {
            const active = activeFilters.has(f.id);
            return (
              <button
                key={f.id}
                type="button"
                onClick={() => toggleFilter(f.id)}
                className={`rounded-full border px-3 py-1 text-xs font-semibold transition ${active ? "border-sky-950 bg-sky-950 text-white" : "border-slate-300 bg-white text-slate-600 hover:border-slate-400"}`}
              >
                {f.label}
              </button>
            );
          })}
          {activeFilters.size > 0 && (
            <button type="button" onClick={() => setActiveFilters(new Set())} className="text-xs text-slate-400 underline hover:text-slate-600">
              Clear filters
            </button>
          )}
        </div>

        <div className="mb-4 flex items-center justify-between">
          <span className="text-xs font-semibold text-slate-400">{visibleProducts.length} product{visibleProducts.length !== 1 ? "s" : ""}</span>
          <div className="flex items-center gap-2">
            <button onClick={() => scroll("left")} aria-label="Scroll left" className="flex h-9 w-9 items-center justify-center rounded-full border border-slate-200 bg-white text-slate-500 shadow-sm transition hover:border-sky-300 hover:text-sky-950"><ChevronLeft size={16} /></button>
            <button onClick={() => scroll("right")} aria-label="Scroll right" className="flex h-9 w-9 items-center justify-center rounded-full border border-slate-200 bg-white text-slate-500 shadow-sm transition hover:border-sky-300 hover:text-sky-950"><ChevronRight size={16} /></button>
          </div>
        </div>

        <div ref={scrollRef} className="flex gap-5 overflow-x-auto pb-4 scroll-smooth" style={{ scrollbarWidth: "none", msOverflowStyle: "none" } as React.CSSProperties}>
          {visibleProducts.map((product) => (
            <div key={product.name} className="flex-none" style={{ width: "calc(33.333% - 14px)", minWidth: "300px" }}>
              <div className="flex h-full flex-col overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm" style={{ borderLeft: `4px solid ${product.accentColor}` }}>
                <div className="border-b border-slate-100 bg-slate-50 px-5 py-4">
                  <div className="flex items-center justify-between gap-2">
                    <span className="inline-flex items-center rounded bg-slate-100 px-2 py-0.5 text-[10px] font-bold uppercase tracking-widest text-slate-600">{product.fullLabel}</span>
                    <div className="flex shrink-0 items-center gap-1">
                      {product.tdsUrl && (
                        <a href={product.tdsUrl} target="_blank" rel="noopener noreferrer" className="inline-flex items-center gap-1 rounded-lg border border-slate-200 bg-white px-2.5 py-1 text-[10px] font-semibold text-slate-500 transition hover:border-slate-300 hover:text-slate-700"><FileText size={9} /> TDS</a>
                      )}
                      <a href={product.brandUrl} target="_blank" rel="noopener noreferrer" className="inline-flex items-center gap-1 rounded-lg border border-slate-200 bg-white px-2.5 py-1 text-[10px] font-semibold text-slate-500 transition hover:border-slate-300 hover:text-slate-700"><ExternalLink size={9} /> Brand Site</a>
                    </div>
                  </div>
                  <h3 className="mt-2 text-sm font-extrabold leading-snug text-sky-950">{product.name}</h3>
                  <p className="mt-0.5 text-[10px] font-bold uppercase tracking-wider text-red-700">{product.productType}</p>
                  <CollapsibleCardDetails text={product.descriptionLine} chips={product.techChips} />
                </div>
                <div className="border-b border-sky-100 bg-sky-50 px-5 py-4">
                  <p className="mb-2 text-[10px] font-bold uppercase tracking-wider text-sky-700">System Description</p>
                  <CollapsibleDescription text={product.systemDescription} />
                </div>
                <div className="space-y-3 px-5 py-4">
                  <div>
                    <p className="mb-2 text-[10px] font-bold uppercase tracking-wider text-green-700">Technical Properties</p>
                    <CollapsibleList items={product.technicalProperties} icon="check" limit={3} />
                  </div>
                  <div>
                    <p className="mb-2 text-[10px] font-bold uppercase tracking-wider text-red-700">Limitations</p>
                    <CollapsibleList items={product.limitations} icon="x" limit={3} />
                  </div>
                </div>
                <div className="mt-auto border-t border-slate-100 bg-slate-50 px-5 py-3">
                  <CollapsibleSources sources={product.procurementSources} />
                </div>
              </div>
            </div>
          ))}
        </div>
      </div>
    </>
  );
}
